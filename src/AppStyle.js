import { StyleSheet } from 'react-native';

module.exports = StyleSheet.create({
  App: {
    // textAlign: 'center',
    marginTop: 20,
    flex: 1,
    overflow: 'visible',
  },

  AppTitle: {
    fontSize: 10,
  },

  AppIntro: {
    fontSize: 20,
  },
});
